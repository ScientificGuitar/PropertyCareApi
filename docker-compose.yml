services:
  postgres:
    image: postgres:16
    container_name: propertycare-postgres
    environment:
      POSTGRES_DB: propertycare
      POSTGRES_USER: propertycare
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: